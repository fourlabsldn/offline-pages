// RequireJS
import './require';
// Custom requireJS plugin to load dependencies cacheFirst
import './cacheFirst';

// Our dynamic-pages will need this, so we better keep it here.
import './handlebars'
